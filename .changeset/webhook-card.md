---
"@gryt/ui": minor
---

Add `WebhookCard`, which draws a card a webhook posted: author, title, description, fields, a thumbnail, an image, a footer and a timestamp. The payload colour only draws a dot, so a webhook can't make its card unreadable. Pass `renderMarkdown` for the description and field values.
