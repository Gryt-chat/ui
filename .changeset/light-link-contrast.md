---
"@gryt/theme": patch
---

The light theme's accentLight token, which every link colour reads from, shared the dark value (#b4afff) — about 2:1 on a white or near-white page. Light now has its own value, #4e42ff, the same violet at roughly 5.9:1 on `surface` and 5.3:1 on `bg`.
