---
"@gryt/ui": minor
---

Adds `VideoPlayer`, a video player for chat attachments. It shows the poster and loads nothing until play is pressed, unless `autoLoad` is set. Volume is controlled with `volume` (0 to 100) and `onVolumeChange`. Space or K plays and pauses, the arrow keys skip 5 seconds, M mutes and F goes fullscreen.
