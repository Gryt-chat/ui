---
"@gryt/ui-native": minor
---

Add `WebhookCard`, the card a webhook message can carry. It's the compact look: a thin border, a coloured dot beside the author, fields as small chips, a full-width image and a muted footer. Links go through `onOpenUrl`, and markdown through `renderMarkdown` when the host passes one.
