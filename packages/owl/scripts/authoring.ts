/**
 * The extractor plus this repository's ink table, for anything authoring a cosmetic. Under
 * scripts/ rather than src/, because src/ is what gets published and this half is not.
 */

export * from "../src/lib/extract";
export * from "../src/lib/owl-group";
export { INKS } from "../artwork/inks";
