import { Composer, ConversationItem, MessageBubble, Surface } from "@gryt/ui";
import { Preview } from "../components/Preview";

# Chat

Gryt-specific chat primitives for desktop conversation layouts.

<Preview>
  <div className="grid gap-4 lg:grid-cols-[18rem_1fr]">
    <Surface className="space-y-1 p-3" elevated>
      <ConversationItem title="Gryt UI" subtitle="Component system" active />
      <ConversationItem title="Design tokens" subtitle="Code-theme palette" />
      <ConversationItem title="Publishing" subtitle="npm package" />
    </Surface>
    <Surface className="space-y-4 p-4" elevated>
      <MessageBubble from="assistant">
        Material 3-style shape is active.
      </MessageBubble>
      <MessageBubble from="user">Use the Gryt code-theme colors.</MessageBubble>
      <Composer submitLabel="Send" />
    </Surface>
  </div>
</Preview>

```tsx
import { Composer, ConversationItem, MessageBubble, Surface } from "@gryt/ui";

<Surface>
  <ConversationItem title="Gryt UI" subtitle="Component system" active />
  <MessageBubble from="assistant">
    Material 3-style shape is active.
  </MessageBubble>
  <Composer submitLabel="Send" />
</Surface>;
```
